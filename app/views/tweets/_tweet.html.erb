  <div class="tweet border-1">
    <div class="">
      <% if tweet.user.avatar.attached? %>
        <%= link_to (image_tag tweet.user.avatar, class: "avatar"), user_path(tweet.user) %>
      <% else %>
        <%= link_to (image_tag "icon/avatar.svg", class: "avatar"), user_path(tweet.user) %>
      <% end %>
    </div>

    <div id="<%= dom_id tweet %>" class="tweet__content">
      <div class="tweet__header">
        <h4 class="content-m bold"><%= link_to (tweet.user.name), user_path(tweet.user) %></h4>
        <span class="content-m color-secondary">@<%= link_to (tweet.user.username), user_path(tweet.user)  %></span>
        <span class="content-s color-secondary"><%= time_ago_converter(time_ago_in_words(tweet.created_at)) %></span>
      </div>

      <p class="content-m color-black"><%= link_to (tweet.body), tweet_path(tweet) %> </p>

      <div class="tweet__footer">
        <div class="flex gap-2 center-ver center-hor">
          <%= image_tag "icon/cloud.svg" %>
          <span class="content-m color-secondary"><%= tweet.replies_count %></span>
          <% if current_user && (@tweets_of_current_user&.include? tweet) %>
            <%= button_to (image_tag "icon/loved_red.svg"), tweet_likes_path(tweet), class: "whithout_formate" %>
            <span class="content-m color-secondary"><%= tweet.likes_count %></span>
          <% else %>
            <%= button_to (image_tag "icon/loved.svg"), tweet_likes_path(tweet), class: "whithout_formate" %>
            <span class="content-m color-secondary"><%= tweet.likes_count %></span>
          <% end%>
        </div>

        <div class="flex gap-2">
          <%=link_to (image_tag "icon/trash.svg", class: "icon"), tweet_path(tweet), data: {turbo_method: :delete} %>
          <%=link_to (image_tag "icon/edit.svg", class: "icon"), tweet_path(tweet) %>
        </div>
      </div>
    </div>

  </div>
